# Copy this to .env and fill in your real key. Never commit the real .env file,
# and never paste the real key into chat/code — set it here or in your host's
# environment variables dashboard only.
GEMINI_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
